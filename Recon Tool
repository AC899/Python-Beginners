import pyfiglet
import sys
import socket
import time
import os
import requests

#logo / banner
ascii_banner = pyfiglet.figlet_format("RECON TOOL", font = "slant")
print(ascii_banner + "Automating Enumeration since 2022")

time.sleep(0.5)

#nslookup
#Use Pyfiglet to display results differently for ns to dirbuster 
nsip = input("\nEnter your target website [example: google.com] : ")
time.sleep(0.5)
print(os.system('nslookup ' + nsip))


#directorybuster tool
# Entering path to the wordlists for Fuxxing 

filename = input("Enter the path of the wordlist you want to use for directory busting: ")
out = os.path.isfile(filename)   #verifies that the path provided in filename is a filename on the local system


#loop needs to work correctly so as that it accepts False a fwe times and then registers a True
while (out ==False):
    filename = input("This directory does not exist - please enter the full file path i.e. C:/Desktop/wordlist.doc")
else:
    print("The wordlist you have chosen is " + filename)
